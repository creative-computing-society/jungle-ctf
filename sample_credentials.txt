#secret key for settings.py
SECRET_KEY='some_super_random_string'

#debug variable for settings.py
DEBUG=True

#Database Credentials
DB_NAME='escalade'        #database name
DB_USER='postgres'        #database user that has access to database
DB_PASSWORD='password'    #password for that database user
DB_HOST='localhost'       #host name for postgres dbms
DB_PORT=5432              #Port on which Postgres is running

#Email ID Credentials (for sending password to registered teams)
EMAIL_HOST_USER='email@gmail.com'         #Gmail ID
EMAIL_HOST_PASSWORD='abcdefghijklmnop'    #App password for the Google Account
